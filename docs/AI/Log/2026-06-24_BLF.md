# AI use log 2026-06-24

On 2026-06-18 I learned that I needed to get the Configuration Entity finished before any others could
be realistically designed and built. 

I will now try the same prompt from the 18th, but updated for Configuration.

```
Consider this design document:

gemsModules/configuration/Design_Doc.md

Consider these codebase documents in docs/:
Architecture.md  Coventions.md  Data_Flow.md  Design_Notes.md
giving slightly more weight to Conventions.md and Data_Flow.md because they have been edited more recently.

As needed, see any other modules in gemsModules/ and in gRPC/ noting that other modules cannot be completed 
before this one.

Tell me what additional infomation you require before being able to complete the configuration module according 
to its Design Document.
```

Things are much better this time. 

I need to:

1. Typos and such
  - [x] Change all SupportedExecutionContexts instances to become SupportedServices.
        First, I should ensure that this is the correct correlation. 
  - [x] State that the InstanceConfig in the deprecated folder is for reference only. 
        No deprecated modules use it. All non-deprecated should be updated as needed.
        When finished, the deprecated version should no longer be in use at all.
  - [x] Provide mappings and additional information for keys that have changed (see response).

2. External Services
  - [x] Explain how to use Marco by delegation via gRPC/JSON.
  - [x] Explain how Delegator's Check Configuration Service is expected to work and be used.

3. User Script
  - [ ] Write the list of questions for the Q/A TUI with validatio rules.
  - [ ] Use markdown for the friendly format. Make an example.
 
I asked for interim info before I completed item 3. See the reference doc for more info.

I gotta go rest for now, but approximate answers are:

1. Process clarifications
  - [ ] It wants to know how to write proper code for my pseudo-code. I'm not sure I care which method.
  - [ ] I need to make plain that the services we provide are a totally different thing from the info needed
        by a scheduler (e.g., Slurm). I'll add that to the docs. 
  - [ ] I need to explain that the Slurm parts of Batch Compute will know how to talk to Slurm. The IC 
        only needs to contain information that is specific to a given installation. I will also add this
        to the docs.

2. How to do the API in IC
  - [ ] I think my answer to its question is to make a helper method for InstanceConfig. I want to avoid 
        having IC be a child process of anything in Common. 
  - [ ] I forgot to say that the API calls need to go through gRPC. I will say that and explain how to.
